services:
  dbservice:
    ports:
      - 127.0.0.1:3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: ROOT
      MYSQL_DATABASE: jobportal

    volumes:
      - mysql_data:var/lib/mysql
volumes:
  mysql_data:
